<Window x:Class="FileOrganizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileOrganizer"
        xmlns:vm="clr-namespace:FileOrganizer.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <TextBox Text="{Binding FolderPath}" IsReadOnly="True"/>
        <Button Content="Select Folder" Command="{Binding SelectFolderCommand}" Margin="0, 5"/>

        <ListView ItemsSource="{Binding Files}" Height="300" Margin="0,5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="30">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Icon}" Width="16" Height="16"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Type" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Button Content="Select Folder" Command="{Binding SelectFolderCommand}" Width="100"/>
            <TextBox Width="100" Margin="5,0,0,0" Text="{Binding FileType, UpdateSourceTrigger=PropertyChanged}" 
            />
            <Button Content="Organize Files" Command="{Binding MoveFilesCommand}" Width="120" Margin="5,0,0,0"/>
        </StackPanel>

        <Button Content="Organize Files" Command="{Binding MoveFilesCommand}" Margin="0,5"/>

    </StackPanel>
</Window>
