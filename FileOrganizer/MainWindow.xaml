<Window x:Class="FileOrganizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileOrganizer"
        xmlns:vm="clr-namespace:FileOrganizer.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Button Content="Select Folder" Command="{Binding SelectFolderCommand}" Width="120"/>
            <CheckBox Content="Organize All Files" IsChecked="{Binding OrganizeAll}" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <ListBox Width="120" Height="100" ItemsSource="{Binding FileTypes}" SelectionMode="Multiple"
                SelectionChanged="ListBox_SelectionChanged"/>
            <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                <TextBox Width="100" Text="{Binding CustomFileType}"/>
                <Button Content="Add" Command="{Binding AddCustomFileTypeCommand}" Margin="5,0,0,0"/>
            </StackPanel>
            <Button Content="Organize Files" Command="{Binding MoveFilesCommand}" Width="120" Margin="10,0,0,0"/>
        </StackPanel>

        <ListView ItemsSource="{Binding Files}" Height="300">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="30">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Icon}" Width="16" Height="16"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Type" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>


</Window>
